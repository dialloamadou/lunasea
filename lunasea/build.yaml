targets:
  $default:
    sources:
      - lib/**
      - pubspec.*
      - $package$
    builders:
      # Hive
      hive_ce_generator:
        enabled: true
      hive_ce_generator:hive_registrar_generator:
        enabled: false
      # JSON Serializable
      json_serializable:
        enabled: true
        options:
          explicit_to_json: true
          include_if_null: false

      # Retrofit
      retrofit_generator:
        enabled: true
